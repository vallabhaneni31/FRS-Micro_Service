/**
 * App.tsx — W-01: superseded by src/app/router.tsx
 *
 * Routing, auth guards, and layout now live in router.tsx.
 * This file is kept only so any legacy import of `App` still resolves;
 * the real entry point is main.tsx → RouterProvider(router).
 */
export { default } from './router';
